{
  "name": "url-parsing",
  "version": "0.1.8",
  "description": "Parses urls and canonical urls",
  "main": "src/index.js",
  "engines": {
    "node": ">=6.0"
  },
  "scripts": {
    "test": "mocha ./tests",
    "test:watch": "mocha ./tests -w",
    "precommit": "npm test",
    "prepush": "npm test",
    "prepublish": "echo \"Pre-publish hook TBD\" # This should have something",
    "publish:patch": "npm version patch && git push --tags",
    "publish:minor": "npm version minor && git push --tags",
    "publish:major": "npm version major && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jermspeaks/url-parsing.git"
  },
  "keywords": [
    "url",
    "canonical",
    "urls",
    "parsing"
  ],
  "author": "Jeremy Wong (jermspeaks@gmail.com)",
  "license": "GPL-2.0",
  "bugs": {
    "url": "https://github.com/jermspeaks/url-parsing/issues"
  },
  "homepage": "https://github.com/jermspeaks/url-parsing#readme",
  "devDependencies": {
    "expect": "^1.20.2",
    "husky": "^0.11.6",
    "mocha": "^2.5.3"
  }
}
